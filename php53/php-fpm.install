post_install() {
	if [[ ! -d opt/php53/shared/run ]]; then
		usr/bin/systemd-tmpfiles --create php53-fpm-shared.conf
	fi
	if [[ ! -d opt/php53/bundled/run ]]; then
		usr/bin/systemd-tmpfiles --create php53-fpm-bundled.conf
	fi
}

post_upgrade() {
	post_install
}
